extends layout.pug

block content
  table
    thead
      tr
        th Id
        th Name
        th Visits
    each visitor in visitors
      tbody
        tr
          td= visitor.id
          td= visitor.name
          td= visitor.count
